using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MyVars : MonoBehaviour
{
    public static int groundSpeed = 0;
    public static int stopMoving = 1;

    //float timeSpeed = 20f;

    //public int showTime = 0;
    void Start() { }
    void Update()
    {
        /*
        timeSpeed -= Time.deltaTime;
        if (timeSpeed < 0)
        {
            timeSpeed = 20;
            groundSpeed += 1;
            showTime = groundSpeed;
        }
        */

    }



}
